
/**
 *
 * @param {object} {description, config} The Honey description and the user config
 * @returns {whatever you want}
 */

export default function honeyDipper({ description, config }) {
  console.log(description, config);
}
